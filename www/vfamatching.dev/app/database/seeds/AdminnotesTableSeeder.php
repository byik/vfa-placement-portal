<?php

class AdminnotesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('adminnotes')->truncate();

		$adminnotes = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('adminnotes')->insert($adminnotes);
	}

}
